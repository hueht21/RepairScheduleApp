import 'package:flutter/material.dart';

class HomeTicket extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const Scaffold(
      body: SafeArea(
        child: Center(
          child: Text("Chúng tôi đnag còn hoàn thiện"),
        ),
      ),
    );
  }



}